rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes (development mode)
    // Users authenticated via email stored in Firestore, not Firebase Auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
